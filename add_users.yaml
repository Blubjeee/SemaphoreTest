---
  host: 192.168.200.71
  connection: network_cli
  gather_facts: no

  vars:
    ansible_network_os: routeros
    ansible_user: admin

  tasks:

      name: Add user
      routeros_command:
        commands:
          - :/user add name=test group=test password=test
